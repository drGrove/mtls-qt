# Mutual TLS Client (mtls) in QT

A QT application for creating mutual TLS certificates using [mtls][mtls]

## Installation ##

### From Pip ###

```
pip3 install --user mtls-qt
```

[mtls]: https://github.com/drGrove/mtls-cli
